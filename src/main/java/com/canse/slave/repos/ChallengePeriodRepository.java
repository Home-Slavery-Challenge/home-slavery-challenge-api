package com.canse.slave.repos;

import com.canse.slave.entities.ChallengePeriod;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChallengePeriodRepository extends JpaRepository<ChallengePeriod,Long> {
}
